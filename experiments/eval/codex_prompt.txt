Faça uma análise manual do arquivo "experiments/eval/outputs/annotation_pool.jsonl". Acesso o conteúdo em blocos de 25 em 25 linhas, leia atentamente as recomendações e avalia de 0...3 a relevância da recomendação. Não utilize nenhum script para te auxiliar nessa tarefa, utilize todo seu conhecimento agregado para avaliar as sugestões. Após a avaliação de cada bloco, salve as respostas no arquivo "experiments/eval/outputs/annotation_pool_categorized.jsonl". Mantenha atenção plena durante todo o processo, sempre utilizando os mesmos critérios para avaliar a relevância das sugestões. 

- Use `relevance = 0` para irrelevante.
- Use `relevance = 1` para relevante.
- Use `relevance = 2` para bastante relevante.
- Use `relevance = 3` para altamente relevante.

Uma recomendação é relevante se a ementa escolhida for relevante para resolver o problema definido na query.