\section{Materiais e Métodos}

\subsection{Visão geral}

O pipeline possui cinco etapas: (i) descoberta de instâncias do SAPL a partir da lista de municípios do IBGE \cite{ibge_localidades_api}; (ii) extração de Projetos de Lei diretamente das APIs públicas; (iii) textualização de ementas; (iv) indexação vetorial para busca semântica; e (v) associação exploratória com indicadores municipais e agrupamento de políticas similares. A Figura~\ref{fig:sonar-pipeline} sintetiza esse fluxo da descoberta das fontes à interface pública.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/pipeline-diagram.jpg}
\caption{Síntese do pipeline do Sonar Municipal, da descoberta de fontes à interface pública.}
\label{fig:sonar-pipeline}
\end{figure*}

\subsection{Descoberta e extração}

A descoberta combina heurísticas de URL com validação por evidência pública. Para cada município, o sistema gera endereços candidatos com base no nome normalizado do município e na sigla da unidade federativa, testa rotas típicas do SAPL e valida a instância quando encontra marcadores compatíveis. Em seguida, a extração deriva a base correta da API, consulta os tipos de matéria e percorre a paginação de todos os tipos cujo rótulo contém ``projeto de lei''.

Na execução congelada em 12 de novembro de 2025, foram encontradas 1.259 instâncias do SAPL, das quais 322 permitiram extração válida com ao menos um PL, totalizando 220.065 registros. Considerando os 5.570 municípios brasileiros, isso corresponde a 22,60\% de cobertura na descoberta e 5,78\% na extração automatizada. A diferença entre essas etapas reflete heterogeneidade técnica entre portais, mudanças de rota, autenticação e diferenças locais de configuração. Assim, a descoberta operou em escopo nacional, mas a base extraída deve ser interpretada como um subconjunto parcial e desigual dos municípios com instâncias SAPL acessíveis.

A base final cobre 322 municípios em 19 UFs. Todos os registros possuem ementa, ação, endereço público de origem e representação vetorial preenchidos. A base não apresentou duplicatas nem no endereço público nem na combinação entre a instância SAPL de origem e o identificador da matéria. O filtro textual de tipos, contudo, ainda recupera cerca de 3,54\% de itens que não correspondem estritamente a PLs, como anteprojetos, emendas, substitutivos e vetos. Esses casos foram mantidos como ruído residual de coleta, sem a pretensão de que a base represente apenas PLs estritos.

\subsection{Textualização de ementas}

Para reduzir ruído jurídico e tornar as proposições mais comparáveis, cada ementa é convertida em uma ação textualizada no formato ``verbo no infinitivo + objeto + complementos essenciais''. O conjunto de treinamento foi composto por 1.000 ementas reais distintas, textualizadas com apoio de GPT-5.1-Thinking \cite{openai_gpt51_model} e revisadas manualmente nos casos em que houve perda de sentido.

Como modelo base, utilizou-se o PTT5-v2 \cite{piau_ptt5v2_2024}, uma variante do T5 \cite{raffel_t5_2020} adaptada ao português. O ajuste fino foi realizado com QLoRA em 4 bits \cite{dettmers_qlora_2023}, com divisão 90/10 entre treino e validação, 30 épocas, taxa de aprendizado de $3\times 10^{-4}$ e tamanho de lote igual a 16. A avaliação automática por BERTScore \cite{zhang_bertscore_2019} atingiu 84\% no conjunto de validação.

No acervo completo, a tradução reduziu o comprimento médio das descrições de 152,86 para 90,01 caracteres e de 24,18 para 13,61 palavras. Isso corresponde a 58,9\% dos caracteres e a 56,3\% das palavras das ementas originais, o que favorece busca e agrupamento. A textualização foi mantida por três razões operacionais. Primeiro, o modelo de representações vetoriais não foi treinado especificamente em texto legislativo. Segundo, ações curtas são mais legíveis para o usuário final do que ementas jurídicas. Terceiro, o agrupamento por similaridade textual tende a funcionar melhor quando há menos ruído introduzido por fórmulas legislativas recorrentes.

\subsection{Busca, indicadores e agrupamento}

As ações são indexadas com representações vetoriais do modelo Multilingual E5 \cite{wang_e5_2024} e armazenadas no Qdrant \cite{qdrant_overview}. As consultas seguem o formato recomendado pelo modelo para entradas de busca. Para avaliar o efeito da textualização na recuperação, comparamos três variantes do mesmo acervo: um \textit{baseline} lexical BM25 sobre as \textit{ementas} originais, busca semântica sobre as \textit{ementas} originais e busca semântica sobre as \textit{ações} textualizadas. O benchmark exploratório foi composto por 15 problemas plausíveis para a plataforma, formulados como necessidades municipais, e por um pool anotado derivado da união dos top-30 das três abordagens. Cada par problema-documento recebeu julgamento automatizado de relevância em escala de 0 a 3 por um agente GPT-5.4, totalizando 1.158 anotações e média de 77,2 candidatos por problema.

Sobre esse pool calculamos P@K, Recall@10, MRR@10, MAP@10, nDCG e métricas restritas a documentos fortemente relevantes ($relevance \geq 2$). Em termos de interpretação, P@K mede a fração de itens úteis nas primeiras posições; Recall@10 mede quantos itens relevantes do pool aparecem no top-10; MRR@10 privilegia a posição do primeiro acerto; MAP@10 resume precisão e ordenação ao longo do ranking; e nDCG usa relevância graduada para premiar listas que colocam os melhores documentos mais cedo. As métricas ``High'' repetem essa lógica considerando apenas documentos com relevância alta. Como o conjunto anotado deriva da união dos rankings comparados, a avaliação mede qualidade relativa de ordenação e cobertura local do top-30 anotado, e não \textit{recall} absoluto no corpus completo. Em outras palavras, o experimento responde com mais segurança à pergunta ``ações e ementas superam o baseline lexical neste pool avaliado?'' do que à pergunta mais forte sobre superioridade robusta e universal no corpus real.

O uso de GPT-5.4 nessa etapa teve finalidade instrumental: padronizar os julgamentos de relevância e viabilizar uma comparação rápida entre métodos. Esse agente não substitui avaliadores humanos especialistas nos domínios substantivos cobertos pelas consultas, mas sua saída já fornece uma referência base consistente para comparar a qualidade relativa entre \textit{bm25}, \textit{ementas} e \textit{ações}.

Para aproximar o sistema de desfechos observáveis, utilizamos taxa de homicídios por 100 mil habitantes e taxa de matrículas em ensino regular por 100 mil habitantes. As variações são calculadas comparando o indicador na data mais próxima ao PL com janelas posteriores de 6, 12, 18, 24, 30 e 36 meses. Trata-se de uma análise descritiva, sem pretensão causal, utilizada apenas para priorização exploratória de políticas recorrentes.

PLs semanticamente próximos são agrupados por similaridade de Jaccard sobre o texto da ação, com limiar 0,75. Para cada grupo, definimos o seguinte escore heurístico:

\[
Q = \frac{p}{n} \times \frac{n}{n+1}
\]

em que $n$ é o número de municípios representados e $p$ é o número de casos com variação favorável ao objetivo do indicador. O termo final penaliza grupos muito pequenos e ajuda a priorizar políticas mais recorrentes.

\subsection{Implementação}

A plataforma foi implementada em React/Next.js e integra interface web, serviço de geração de representações vetoriais, banco vetorial e dados de indicadores \cite{vercel_nextjs}. O código de coleta, treinamento, indexação e interface permanece disponível em \url{https://github.com/thiagoambiel/SonarMunicipal}, juntamente com o prompt do tradutor e a lista congelada de instâncias SAPL utilizadas no artigo. A implementação web pública está disponível em \url{https://sonar-municipal.vercel.app/}. A reprodução integral do acervo depende da disponibilidade externa das APIs públicas no momento da coleta, mas o repositório documenta as etapas e disponibiliza os artefatos necessários para reexecução.
